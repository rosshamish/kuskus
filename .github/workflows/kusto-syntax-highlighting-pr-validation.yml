name: kusto-syntax-highlighting-pr-validation

on:
  pull_request:
    branches:
      - master
    paths:
      - 'kusto-syntax-highlighting/**'

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:


jobs:
  validate:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: kusto-syntax-highlighting

    steps:
      # TODO xml validation, or otherwise tmlanguage validation. Right
      # now this is a no-op validation run.
      - uses: actions/checkout@v2
